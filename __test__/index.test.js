/* import { screen, render } from "@testing-library/react";

describe("Given a component Home", () => {
  describe("When it's invoked", () => {
    test("Then it should render a <p> with 'POKEMON' inside", () => {
        render(Home); 
    });
  });
});
 */
